node {

    try {
     
        stage ('Ansible') {
            
            credentialsId: 'srv_id',
            ansiblePlaybook colorized: true, 
            installation: 'ansible', 
            inventory: 'hosts', 
            playbook: 'install_environment.yml', 
            tags: 'git'

        }
    
    } 
    
    catch(ex) {
                    
        error("Stage Ansible GIT failure!");
                    
    }

}
